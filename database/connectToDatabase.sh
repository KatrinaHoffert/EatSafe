#!/usr/bin/env bash

# Connects to the remote database with a Postgres client. psql must be in the PATH.
export PGPASSWORD=M4PGx3zUySvMmB9j

psql --host=cmpt371g1.usask.ca --dbname=eatsafe --username=eatsafe